# ==============================================================
# yudao-cloud 本地开发 Docker 基础设施配置
# ==============================================================
#
# 端口规划（主机 → 容器，避免与本地已运行服务冲突）：
#   MySQL:           13306 → 3306
#   Redis:           16379 → 6379
#   Nacos Server:    18848 → 8848
#   Nacos Console:   18080 → 8080
#   Nacos gRPC:      19848 → 9848
#   RocketMQ NameSrv:19876 → 9876
#   RocketMQ Broker: 20911 → 10911
#   RocketMQ Proxy:  20909 → 10909
#   XXL-Job Admin:   19090 → 8080
#
# 提示：如果本地没有运行这些服务，可以直接使用默认端口（如 3306:3306），
#       这样无需修改 application-local.yaml 中的配置。
#       如果使用以上非标准端口，则需要同步修改各模块 application-local.yaml
#       中对应的 host:port 地址。

# MySQL 数据库密码
MYSQL_ROOT_PASSWORD=123456

# Redis 密码（本地开发不设密码，如需密码请取消下面的注释）
# REDIS_PASSWORD=123456

# Nacos 认证（本地开发不开启，如需开启请取消下面的注释）
# NACOS_AUTH_ENABLE=true